# ogg-opus-concat
Concatenates several Ogg-containered Opus files into one file without re-encoding, preserving all the data from the first on intact, so each new file only appends to the data before it.
